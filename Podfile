# Uncomment the next ine to define a global platform for your project
# platform :ios, '9.0'

target 'JudgementDay' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for JudgementDay
     pod 'SwiftyPickerPopover'
     pod 'AWSCognito'
     pod 'AWSCognitoIdentityProvider'
     pod 'AWSLambda'

  target 'JudgementDayTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'JudgementDayUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
